# Minimal dependencies required by the Vercel serverless API.
# Heavy desktop/AI/CV packages are intentionally excluded to keep the
# deployment bundle below Vercel's 500 MB ephemeral storage limit.
fastapi
uvicorn
pydantic
python-dotenv
groq
requests
